This directory contains unit test for depthcharge module.

Where possible, tests should be organized in a heirarchy that mirrors that of
the module.

To run all tests:

```
python3 -m unittest unit_tests
```
